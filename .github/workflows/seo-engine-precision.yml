name: "🎯 Precisión del motor SEO"

on:
  schedule:
    - cron: '20 9 * * 4'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: seo-engine-precision
  cancel-in-progress: true

jobs:
  precision:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v6

      - name: "🧮 Calcular precisión"
        run: |
          set -euo pipefail
          python3 .github/scripts/seo_engine_precision.py --json /tmp/seo-engine-precision.json --report /tmp/seo-engine-precision.md
          cat /tmp/seo-engine-precision.md

      - name: "📝 Sincronizar issue canónico"
        uses: actions/github-script@v8
        env:
          REPORT_PATH: /tmp/seo-engine-precision.md
          JSON_PATH: /tmp/seo-engine-precision.json
        with:
          script: |
            const fs = require('fs');
            const titlePrefix = '🎯 Precisión del motor SEO';
            const today = new Date().toISOString().slice(0, 10);
            const body = [
              fs.readFileSync(process.env.REPORT_PATH, 'utf8'),
              '',
              '```json',
              fs.readFileSync(process.env.JSON_PATH, 'utf8'),
              '```',
            ].join('\n');

            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'report',
              per_page: 100,
            });

            const matching = issues
              .filter((issue) => issue.title.startsWith(titlePrefix))
              .sort((a, b) => a.number - b.number);
            const title = `${titlePrefix} — ${today}`;

            if (matching.length > 0) {
              const primary = matching[0];
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: primary.number,
                title,
                body,
              });
              for (const duplicate of matching.slice(1)) {
                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: duplicate.number,
                  state: 'closed',
                });
              }
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ['report'],
              });
            }
