name: "🧪 Precisión SEO por tipo de cambio"

on:
  schedule:
    - cron: '35 9 * * 4'

permissions:
  contents: read
  issues: write

concurrency:
  group: seo-precision-by-change
  cancel-in-progress: true

jobs:
  report:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v6

      - name: "🧮 Generar reporte"
        run: |
          set -euo pipefail
          python3 .github/scripts/seo_precision_by_change_type.py --json /tmp/seo-precision-by-change.json --report /tmp/seo-precision-by-change.md
          cat /tmp/seo-precision-by-change.md

      - name: "📝 Sincronizar issue canónico"
        uses: actions/github-script@v8
        env:
          REPORT_PATH: /tmp/seo-precision-by-change.md
          JSON_PATH: /tmp/seo-precision-by-change.json
        with:
          script: |
            const fs = require('fs');
            const titlePrefix = '🧪 Precisión SEO por tipo de cambio';
            const today = new Date().toISOString().slice(0, 10);
            const body = [
              fs.readFileSync(process.env.REPORT_PATH, 'utf8'),
              '',
              '```json',
              fs.readFileSync(process.env.JSON_PATH, 'utf8'),
              '```',
            ].join('\n');

            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'report',
              per_page: 100,
            });

            const matching = issues
              .filter((issue) => issue.title.startsWith(titlePrefix))
              .sort((a, b) => a.number - b.number);
            const title = `${titlePrefix} — ${today}`;

            if (matching.length > 0) {
              const primary = matching[0];
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: primary.number,
                title,
                body,
              });
              for (const duplicate of matching.slice(1)) {
                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: duplicate.number,
                  state: 'closed',
                });
              }
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ['report'],
              });
            }
