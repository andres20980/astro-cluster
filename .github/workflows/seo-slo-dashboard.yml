name: "🧭 SEO SLO dashboard"

on:
  schedule:
    - cron: '15 10 * * 4'

permissions:
  contents: read
  issues: write

concurrency:
  group: seo-slo-dashboard
  cancel-in-progress: true

jobs:
  slo:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: "🧮 Generar inputs"
        run: |
          set -euo pipefail
          python3 .github/scripts/seo_cluster_scorecard.py --json /tmp/seo-scorecard.json --report /tmp/seo-scorecard.md
          python3 .github/scripts/check_query_cannibalization.py --json /tmp/seo-cannibalization.json --report /tmp/seo-cannibalization.md --fail-threshold 999
          python3 .github/scripts/seo_engine_precision.py --json /tmp/seo-engine-precision.json --report /tmp/seo-engine-precision.md

      - name: "🧭 Construir dashboard"
        run: |
          set -euo pipefail
          python3 .github/scripts/seo_slo_dashboard.py \
            --precision /tmp/seo-engine-precision.json \
            --scorecard /tmp/seo-scorecard.json \
            --cannibal /tmp/seo-cannibalization.json \
            --json /tmp/seo-slo.json \
            --report /tmp/seo-slo.md
          cat /tmp/seo-slo.md

      - name: "📝 Sincronizar issue canónico"
        uses: actions/github-script@v8
        env:
          REPORT_PATH: /tmp/seo-slo.md
          JSON_PATH: /tmp/seo-slo.json
        with:
          script: |
            const fs = require('fs');
            const titlePrefix = '🧭 SEO SLO dashboard';
            const today = new Date().toISOString().slice(0, 10);
            const body = [
              fs.readFileSync(process.env.REPORT_PATH, 'utf8'),
              '',
              '```json',
              fs.readFileSync(process.env.JSON_PATH, 'utf8'),
              '```',
            ].join('\n');

            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'report',
              per_page: 100,
            });

            const matching = issues
              .filter((issue) => issue.title.startsWith(titlePrefix))
              .sort((a, b) => a.number - b.number);
            const title = `${titlePrefix} — ${today}`;

            if (matching.length > 0) {
              const primary = matching[0];
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: primary.number,
                title,
                body,
              });
              for (const duplicate of matching.slice(1)) {
                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: duplicate.number,
                  state: 'closed',
                });
              }
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ['report'],
              });
            }
